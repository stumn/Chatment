{
  "name": "chatment",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "deploy": "gh-pages -d dist",
    "postinstall": "cd my-react-app && npm install && npm run build && cp -r dist ../dist"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.13.2",
    "socket.io": "^4.8.1"
  },
  "type": "commonjs",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}